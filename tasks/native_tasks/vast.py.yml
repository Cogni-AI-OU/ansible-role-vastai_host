---
- name: Ensure pip package manager is available (native-tasks)
  ansible.builtin.package:
    name: python3-pip
    state: present
- name: Install vastai CLI package (native-tasks)
  ansible.builtin.pip:
    name: vastai
    executable: pip3
