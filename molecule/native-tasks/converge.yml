---
- name: Converge native tasks mode
  hosts: all
  gather_facts: true
  vars:
    vastai_host_install_mode: native-tasks
    vastai_host_account_identity: dummy-native-tasks-key
  tasks:
    - name: Include main role
      ansible.builtin.include_role:
        name: cogni-ai.vastai_host
